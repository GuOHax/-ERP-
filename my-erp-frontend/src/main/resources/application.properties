# # ========== 数据库配置 ==========
# spring.datasource.url=jdbc:mysql://localhost:3306/myerpdb?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
# spring.datasource.username=root
# spring.datasource.password=asd791226  # 替换为实际密码
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # ========== JPA/Hibernate 配置 ==========
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# # ========== 文件上传配置 ==========
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB
# spring.servlet.multipart.location=uploads  # 保留原始配置的地址

# # ========== 服务器配置 ==========
# server.port=8081
# server.servlet.context-path=/api

# application.properties

# ---------- 数据库配置 ----------
spring.datasource.url=jdbc:mysql://localhost:3306/myerpdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=asd791226
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate 配置
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# ---------- 文件上传配置 ----------
# 上传地址，可自行修改为绝对路径或项目内部静态目录
spring.servlet.multipart.location=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ---------- 服务器端口（可选，默认 8080） ----------
server.port=8081
server.servlet.context-path=/
